<h2>Log in</h2>

<!-- <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name), data: { turbo: :false }) do |f| %>
  <div class="form-inputs">
    <%= f.input :email,
                required: false,
                autofocus: true,
                input_html: { autocomplete: "email" } %>
    <%= f.input :password,
                required: false,
                input_html: { autocomplete: "current-password" } %>
    <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Log in", class: "btn btn-primary" %>
  </div>
<% end %>

<%= render "devise/shared/links" %> -->
<div class="row mx-auto my-5 card-form col-10">
  <div class="col-6 mt-5 d-flex justify-content-center">
  <div class="col-6" id="for1">
  <h3>Welcome back</h3>
  <hr>
   <p><small>Please enter your details</small></p>
  <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name), data: { turbo: :false }) do |f| %>
    <div class="form-inputs">
      <%= f.input :email,
                  required: false,
                  autofocus: true,
                  input_html: { autocomplete: "email" }, input_html: {class:"border-0 border-bottom border-3 bg-transparent"}%>
      <%= f.input :password,
                  required: false,
                  input_html: { autocomplete: "current-password" }, input_html: {class:"border-0 border-bottom border-3 bg-transparent"} %>
      <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
    </div>

    <div class="form-actions">
      <%= f.button :submit, "Log in", class: "btn btn-dark col-12 mb-3" %>
    </div>
  <% end %>

  <%= render "devise/shared/links" %>
  </div>
  </div>
  <div class="img-form1 col-6">
    <div class="glassmorphism">

    <h3>Life's too short to wear boring clothes!</h3>
    <p>Find in <strong>Referent</strong> unique garments from the best designers</p>

</div>
