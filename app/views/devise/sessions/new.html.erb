<div class="container mx-auto">
<div class="row mx-auto my-5 card-form col-sm-12 col-lg-10">
  <div class="col-sm-12 col-lg-6 mt-5 d-flex justify-content-center">
  <div class="mx-auto">

  <h3 class="text-center">Welcome!</h3>

<p class="text-center">¿Don't have an account?</p>
     <%= link_to "Sing Up", new_user_registration_path, class:"btn btn-danger col-12 text-light" %>

  <hr>
   <p><small>Please enter your details</small></p>
  <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name), data: { turbo: :false }) do |f| %>
    <div class="form-inputs">
      <%= f.input :email,
                  required: false,
                  autofocus: true,
                  input_html: { autocomplete: "email" }, input_html: {class:"border-0 border-bottom border-1 bg-light border-dark"}%>
      <%= f.input :password,
                  required: false,
                  input_html: { autocomplete: "current-password" }, input_html: {class:"border-0 border-bottom border-1 bg-transparent border-dark"} %>
      <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
    </div>

    <div class="form-actions">
      <%= f.button :submit, "Log in", class: "btn btn-dark col-12 mb-3" %>
    </div>
  <% end %>

  <%= render "devise/shared/links" %>
  </div>
  </div>
  <div class="img-form1 col-sm-6 col-lg-6">

    <div class="glassmorphism">


    <h3>Life's too short to wear boring clothes!</h3>

    <p>Find in <strong>Referent</strong> unique garments from the best designers</p>

</div>
</div>
